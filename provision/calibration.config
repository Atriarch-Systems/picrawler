# robot-hat config and calibration value of robots
# atriarch-spider-quad servo offsets — per-robot-BODY, valid across Pi swaps.
# Re-run `python3 examples/0_calibration.py` (NO sudo) only if the body/servos change.

picrawler_servo_offset_list = [0.0, 0.0, 0.0, -0.41, -8.62, -5.23, 5.27, 4.75, 3.2, 8.8, -3.75, -4.23]
